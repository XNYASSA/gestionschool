services:
  - type: web
    name: gestionschool-backend
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm run dev
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        value: file:./dev.db
      - key: JWT_SECRET
        value: your_jwt_secret_change_this_in_production_12345
      - key: FRONTEND_URL
        value: https://gestionschool.vercel.app
    staticSite: false
    previewUrl: false
    region: oregon
